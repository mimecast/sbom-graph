apiVersion: v1
kind: Service
metadata:
  name: {{ include "sbom-graph-api.fullname" . }}
  labels:
    {{- include "sbom-graph-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sbom-graph-api.selectorLabels" . | nindent 4 }}
