apiVersion: v1
kind: Service
metadata:
  name: {{ include "sbom-graph.sbomGraphApi.fullname" . }}
  labels:
    {{- include "sbom-graph.sbomGraphApi.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.sbomGraphApi.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sbom-graph.sbomGraphApi.selectorLabels" . | nindent 4 }}
