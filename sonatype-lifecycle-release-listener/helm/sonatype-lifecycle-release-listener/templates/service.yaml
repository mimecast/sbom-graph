apiVersion: v1
kind: Service
metadata:
  name: {{ include "sonatype-lifecycle-release-listener.fullname" . }}
  labels:
    {{- include "sonatype-lifecycle-release-listener.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sonatype-lifecycle-release-listener.selectorLabels" . | nindent 4 }}
