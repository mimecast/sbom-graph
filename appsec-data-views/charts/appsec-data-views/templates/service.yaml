apiVersion: v1
kind: Service
metadata:
  name: {{ include "appsec-data-views.fullname" . }}
  labels:
    {{- include "appsec-data-views.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "appsec-data-views.selectorLabels" . | nindent 4 }}
